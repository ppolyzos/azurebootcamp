@model Prize[]
<section id="giveaways">
    <div class="container">
        <div class="section-heading scrollpoint sp-effect3">
            <h1>Awesome giveaways!</h1>
            <div class="divider"></div>
        </div>
        <div class="row"> </div>
        <!-- row -->
        <div class="row">
            @foreach (var giveaway in Model)
            {
                <div class="col-lg-12 text-center">
                    <a href="@giveaway.ProductLink" target="_blank"><img src="@giveaway.ImageUrl" height="200px"></a> <br>
                    <h2>by <a href="@giveaway.SponsorLink" target="_blank">@giveaway.Name</a></h2>
                </div>
            }
        </div>
        <!-- row -->
    </div>
</section>